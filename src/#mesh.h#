#ifndef MESH_H
#define MESH_h

#include "std.h"

struct mesh_node_t {
  uint32_t chunk_it;
  uint32_t 
  uint8_t is_skel;
  struct list_t nodes;
};

struct mesh_t {
  mesh_node_t 
};

struct chunk_hdr_t {
  uint16_t chunk_id;
  uint16_t len;
};

void load_mesh(char *filename);

#endif
